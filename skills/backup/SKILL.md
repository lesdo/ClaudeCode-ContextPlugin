---
name: backup
description: 触发 Claude Code 全局配置备份 — 执行 backup-claude.sh 创建 ~/.claude/ 的 tar.gz 快照
---

# 备份全局配置

执行 `${CLAUDE_PLUGIN_ROOT}/scripts/backup-claude.sh` 创建 `~/.claude/` 的完整 tar.gz 备份。

用法: `/context-manager:backup [说明文字]`
